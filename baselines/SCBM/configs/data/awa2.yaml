# --------------------------
#         Dataset
# --------------------------
defaults:
  - data_defaults # Inherit data_defaults arguments
dataset: 'awa2' # Name of the dataset 
num_classes: 50 # Number of classes of the target variable
num_concepts: 85 # Number of concepts in dataset
model_type: 'resnet18' # Type of model to use