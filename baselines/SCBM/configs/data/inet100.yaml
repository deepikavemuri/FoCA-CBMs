# --------------------------
#         Dataset
# --------------------------
defaults:
  - data_defaults # Inherit data_defaults arguments
dataset: 'inet100' # Name of the dataset 
num_classes: 100 # Number of classes of the target variable
num_concepts: 700 # Number of concepts in dataset
model_type: "resnet50" # Type of model to use